<%= form_for @task do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    <%= f.text_area :content %>
  </div>
  <%= hidden_field_tag :goal_id, @goal.id %>
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>
